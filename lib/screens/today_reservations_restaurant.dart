import 'package:flutter/material.dart';

class TodayReservationsRestaurant extends StatelessWidget {
  const TodayReservationsRestaurant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Label'),
        
      ),
    );
  }
}
