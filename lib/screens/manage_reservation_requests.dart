import 'package:flutter/material.dart';

class ManageReservationRequests extends StatelessWidget {
  const ManageReservationRequests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Label'),
        
      ),
    );
  }
}
